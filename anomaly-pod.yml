apiVersion: v1
kind: Pod
metadata:
  name: gpu-pod-anomaly
spec:
  containers:
  - name: gpu-container
    image: gitlab-registry.nautilus.optiputer.net/jmduarte/anomalydetection4jets:latest
    command: ["sleep", "infinity"]
    resources:
      limits:
        nvidia.com/gpu: 1